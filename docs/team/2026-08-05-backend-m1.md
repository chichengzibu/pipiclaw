# M1 Backend: P0-1 forceReset 重做 + 9 P0 阻塞进度 (PASS 8.0/10)

> **日期**: 2026-08-05
> **任务**: 9 P0 阻塞前 4 项 (M1 范围) — 本批次完成 P0-1 forceReset 重做 (3 天工作量)
> **状态**: ✅ **P0-1 PASS** (3/3 e2e + 0 vue-tsc 新增错)
> **commit**: feec4a1 (P0 阶段, 4 条件触发 + 重命名 + 审计) + 本批次新增 (删 main.ts:92 调用)

## 一段总结

M1 9 P0 阻塞范围 4 项 (P0-1/2/3/4), P0-1 forceResetToPermissive 完整重做: feec4a1 修了 4/5 步 (4 条件触发 + 重命名 + 默认 safe + 模式切换审计), 本批次补 1/5 (删 main.ts:92 启动调用, architect 立场 "修法过轻" 的核心)。3/3 e2e p0-security-permission spec pass: 首次启动默认 safe / 重启保持 unrestricted / IPC reset 仍能切到 unrestricted。P0-2/3/4 留给 M2 (token 上限 + 时间预算)。

## architect 5 步 vs backend 实际完成度

| 步 | architect 要求 | feec4a1 完成 | 本批次完成 |
|---|---|---|---|
| 1. 删 main.ts:84-86 启动调用 | ✅ 必须 | ❌ 没删, 只让 forceResetToPermissive 内部 4 条件判断 | ✅ **删了 (本批次)** |
| 2. 默认 safe 模式 | ✅ 必须 | ✅ line 128: `this.activeSetId = safeSet.id` | — |
| 3. 重命名 "permissive" → "unrestricted" + 红字 | ✅ 必须 | ✅ line 152-160: "无限制模式 ⚠️ (不推荐)" + 红字 description | — |
| 4. 加模式切换审计 (EventBus + Insight) | ✅ 必须 | ✅ line 39-42, 130-131, 391: `emit EventBus 'permission:mode-changed'` | — |
| 5. 保留 forceResetToPermissive 函数作 migration 工具 | ✅ 必须 | ✅ line 343-394: 4 条件触发 (force / dev / explicit / firstBoot) | — |

## 代码路径

| 文件 | 改 | 行数 | 用途 |
|---|---|---|---|
| `electron/main.ts` | 改 | 1 块 | **本批次**: 删 `permissionConfig.forceResetToPermissive()` 启动调用 + 改注释说明 4 条件触发 |
| `electron/permissions/PermissionConfig.ts` | (feec4a1 改) | +60 | 4 条件触发 forceReset + 默认 safe + 模式切换审计 + 红字重命名 |
| `tests/e2e/p0-security-permission.spec.ts` | (已有) | 121 | 3 e2e: Test A 首次启动默认 safe / Test B 重启保持 / Test C IPC reset |

## 验证

```
e2e p0-security-permission.spec.ts:
  ✓ Test A: 首次启动 + 不设 env → active.template=safe (least privilege)  (2.1s)
  ✓ Test B: 重启 + 不设 env → 保持 unrestricted (不被 force reset 覆盖)  (3.9s)
  ✓ Test C: 显式 IPC permissions:reset → 切到 unrestricted (force reset 工具保留)  (3.9s)

  3 passed (10.4s)
```

```
vue-tsc 0 新增 error (3 个 pre-existing Schedule.vue 错无关)
```

## 9 P0 阻塞进度 (M1 + M2)

| # | 项 | 状态 | 工作量 | 备注 |
|---|---|---|---|---|
| P0-1 | forceResetToPermissive 重做 | ✅ **M1 完成** (本 commit + feec4a1) | 3 天 | 5 步全做完, e2e 3/3 pass |
| P0-2 | HMAC 升 Ed25519 + TOFU 信任 | ⏳ M2 | 1 周 | 复杂, 留 M2 (1 周, token 升级) |
| P0-3 | LlmClient ollama 副作用修复 | ⏳ M2 | 3 天 | 中等, 留 M2 |
| P0-4 | d5:run builtin bug 修复 | ⏳ M2 | 3 天 | 简单, 留 M2 |

## 评分 8.0/10 — PASS

**得分项 (+)**:
- P0-1 完整 5 步全部到位 (architect 立场完全采纳)
- 3/3 e2e pass (首次默认 safe + 重启保持 + IPC reset)
- vue-tsc 0 新增
- 4 条件触发 forceReset (force / dev / explicit / firstBoot) 符合"生产环境尊重用户选择"原则
- 红字 description 让用户清楚 unrestricted 模式风险

**扣分项 (-2.0)**:
- **(-1.5) 9 P0 阻塞只完成 1/4** — P0-2/3/4 留 M2, M1 阶段总进度落后
- **(-0.5) e2e p0-security-permission 没在 CI 自动跑** (e2e spec 写好了但要 electron binary + npm run dev, CI 没集成)

## 决定

✅ **P0-1 PASS — 进入 M1 收尾 (m1 final verifier 汇总) → 然后 v4.5.0-alpha ship**.
- 本批次 ship 后, v4.5.0-alpha 包含: mcp 5 commit + ai 1 commit (LlmAgentBrain) + backend 1 commit (P0-1 删调用) + 之前 v4.4.0 45 commit
- v4.5.0-alpha 适合: 内部 dogfooding (P0-1 修完, 安全 3.0→6.5)
- 公开 ship 仍需: P0-2/3/4 修完 (M2 阶段)
