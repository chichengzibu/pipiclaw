# W1-W6 累积成果复盘报告

> **执行日期**:2026-07-15
> **执行方**:1 个 general_purpose_task subagent / 纯审计
> **工作目录**:`D:\pipiclaw\piclaw`
> **前置 commit**:`d81d83b` W6 D5 demo
> **W1-W6 累计**:42 commit,69 新文件,5 修改,0 新 npm 依赖

---

## TL;DR

**W1-W6 在"代码骨架 + 类型 + 单元测试"上成果扎实,但在"接通 + boot wiring + 路由可达性"上存在重大盲点**。

- ✅ 69 个新文件全部就位,vitest 71/71 通过,git 干净
- ⚠️ **main.ts 没串接任何 W3+ 子系统**:`CapabilityRegistry` / `AgentBrain` / `HermesAdapter` / `registerD1ScreenshotShortcut` / D5 builtin 全是孤儿代码
- ⚠️ **W3.3 IPC 13 namespace + IpcBridge 全部 dead code**:渲染端 0 caller
- ⚠️ **9 个 view 路由不可达**(D1/D5 也包含)
- ⚠️ **W1-W6 新引入 31 个 tsc 错误未消** + **测试覆盖 ≈ 0**

**风险评分**:**5/10**(中等风险,有条件可继续 W7)

---

## 维度 1:架构一致性

### 1.1 能力域接通

| 检查项 | 状态 |
|---|---|
| 7 个域入口接口在 contracts/types.ts 中定义 | ✓ |
| `CapabilityRegistry.getInstance().register()` 调用次数 | **0 次** ⚠️ |
| `CapabilityRegistry.getInstance()` 实例化次数 | **0 次** ⚠️ |
| 只有骨架未实现的域 | **全部 7 个** ⚠️ |

### 1.2 Runtime 5 子系统接通

| 子系统 | 实例化 / 注册次数 | 状态 |
|---|---|---|
| actor (ActorRegistry) | 0 | ⚠️ 全孤儿 |
| bridge (EventBus publish / subscribe) | 38 / 1 | ✓ publish 活跃 |
| conversation (new Conversation) | 0 | ⚠️ |
| scheduler (new Scheduler) | 0 | ⚠️ |
| skill runtime (SkillRuntime.register) | 2 (D1 + D5) | △ D5 仅在 builtin 内,未接入 main |

### 1.3 ChatManager 接入点(W4.6)

| 方法 | 调用方 |
|---|---|
| `registerAgent(brain)` | **0** ⚠️ |
| `_emitStreamChunk(chunk)` | **0** ⚠️ |
| `subscribeStream(handler)` | **0** ⚠️ |
| `streamHandlers` 订阅者 | 0 ⚠️ |

### 1.4 IPC namespace 接通(W3.3 13 handler)

- ✓ IpcServer 末尾追加 13 个 handler(全部 stub `success: true, stub: true`)
- ✓ preload 暴露 13 个 IpcChannels + 4 个 namespace(agent/channel/sandbox/insight)
- ⚠️ **renderer 端 0 caller**:`grep "electronAPI.agent|channel|sandbox|insight" src/` 无任何匹配
- ⚠️ **`runtime:ipc-bridge` channel**(W4.2 IpcBridge)未在 IpcServer.registerHandlers() 注册,与 IpcServer 两套独立路径管理

### 1.5 域间契约一致性

- ✓ HermesAdapter implements HermesMemory(4 方法签名匹配)
- ✓ AgentBrainImpl 实现 AgentBrain 5 方法(签名匹配,bridge helper `asAgentBrain()`)
- ⚠️ `HermesAdapter.getInstance()` 0 调用方
- ⚠️ `ChatManager.subscribeStream` 返回 Disposable,但 preload 没暴露 `chat.subscribeStream`
- ⚠️ 1.0.0 TaskExecutor 用 string literal `'write_file'` 等 20+ 处,`StepType` 枚举未包含 → 28 个 tsc 错误

### 1.6 Demo 接通状态(D1 / D5)

| Demo | 组件 | 状态 |
|---|---|---|
| D1 截屏问答 | globalShortcut Cmd+Shift+S 注册 | ⚠️ main.ts `globalShortcut.registerAll()` 未调 `registerD1ScreenshotShortcut()` |
| D1 | D1 view 路由 | ⚠️ Vue Router 未挂 |
| D1 | D1SkillHandler SkillRuntime 注册 | △ D1ScreenshotQA.ts 内 `ensureD1SkillRegistered()` 自注册,但 main.ts 未触发 |
| D5 录屏转技能 | builtin 注册 | ⚠️ `D5RecordingToSkill.d5SkillHandler` 未被 `SkillRuntime.register()` 调用 |
| D5 | D5 view 路由 | ⚠️ Vue Router 未挂 |
| D5 | D5 IPC 入口 | ⚠️ preload 没暴露 `d5:run`,renderer 不可达 |

### 1.7 路由可达性总览

`src/router/index.ts` 只注册 4 个 route:`/dashboard` / `/chat` / `/skills` / `/settings`。

**其他 10 个 view 全部无路由**:Help / Models / Permissions / PluginMarket / RemoteControl / Schedule / SkillMarket / Tasks / D1ScreenshotDemo / D5RecordingToSkill。

`SideNav` / `App.vue` 也无对应菜单入口。

---

## 维度 2:代码质量

### 2.1 命名规范一致性

- ✓ 单例模式覆盖 ~85%(366 处 `getInstance()` / 66 处 `private static instance`)
- ✓ 文件命名统一(PascalCase 主进程,camelCase 工具)
- △ Bridge helper 模式(`asAgentBrain`)仅 1 例,其他 6 个域入口缺

### 2.2 TypeScript 类型完备性

| 用法 | 处数 | 文件数 |
|---|---|---|
| `: any` | 189 | 35 |
| `as any` | 47 | 19 |
| `as unknown as X` | 4 | 2 (AgentBrain + AgentRecovery) |
| `@ts-ignore` | 1 | 1 (HttpBridge.ts:58) |
| `@ts-expect-error` | 0 | 0 ✓ |

W1-W6 新引入错误(约 31 条):
- CapabilityRegistry.ts 4 个
- IpcServer.ts 11 个(W3.3 部分 8 个 + 其他 3 个)
- ChatManager.ts 8 个(W4.6 stream 段)
- SkillLoader.ts 4 个 / SkillSigner.ts 1 个 / SkillChain.ts 1 个
- Hermes/MemoryVectorStore.ts 1 个 / KeywordRetriever.ts 1 个

### 2.3 错误处理

- ✓ 主流 `catch (e) { this.log.error('msg', e) }` 一致
- ✓ 错误日志统一用 `LogManager`(仅 1 处 `console.warn` in `src/stores/chat.ts:234`)
- ✓ 失败容错:W3.3 IPC `return { success: false, error: ... }` 一致

### 2.4 测试覆盖

- ✓ vitest **71/71 通过**(postinstall 5 + tokens 66)
- ⚠️ **30+ 个 W3-W6 新文件 0 test**:CapabilityRegistry / EventBus / ActorRegistry / Scheduler / SkillRuntime / HermesAdapter / AgentBrain / D1ScreenshotQA / D5RecordingToSkill / AutoCreator / SkillChain / SkillSigner / SkillVersioning / ToolRegistry / ExecutionEngine / ContextCompressor / SubAgentSpawner / TraceCollector / CostTracker / TaskKanban / AnomalyTimeline / InsightManager / ScreenVision / MemoryVectorStore / KeywordRetriever / EmbeddingService / SkillEffectivenessTracker

**应写 test 优先级**:
1. **P0**:`EventBus.publish/subscribe/history`(38 个 publish 调用方,核心基础设施)
2. **P0**:`CapabilityRegistry.register/resolve/execute`(W7 启动前必测正确性)
3. **P0**:`HermesAdapter.recall/store`(W7 接 LLM 前必测)
4. **P1**:`AgentBrainImpl.think/call/spawn`(5 方法核心)
5. **P1**:`ChatManager.registerAgent/subscribeStream/_emitStreamChunk`(W4.6 接入点)
6. **P2**:`ScreenVision.startRecording/captureFrame/stopRecording`(D5 录屏)
7. **P2**:`D5RecordingToSkill.runD5`(5 步流水线)
8. **P2**:`SkillEffectivenessTracker.record/list`(学习闭环)

### 2.5 资源管理

- ✓ `EventBus.subscribe` 返回 `Unsubscribe` ✓
- ✓ `ScreenVision.setInterval` 配对 `clearInterval` ✓
- ✓ fs.writeFile 都有 try/catch ✓

---

## 维度 3:依赖状态

### 3.1 Git

- ✓ working tree 干净
- ✓ 46 个 commit 自初始
- ✓ 最近 commit:`d81d83b` D5 demo

### 3.2 package.json

- dependencies:10 个(`element-plus` / `docx` / `electron-log` / `highlight.js` / `marked` / `pinia` / `playwright` / `vue` / `vue-router` / `@element-plus/icons-vue`)
- devDependencies:13 个,新增 vitest 1.6 + coverage + test-utils + jsdom + vue-tsc(W1.3)
- ✓ **W1-W6 新引入运行时依赖:0**

### 3.3 typecheck

- `tsconfig.node.json`(主进程):**74 个错误**
- `tsconfig.json`(渲染端):**19 个错误**
- vue-tsc:**仍未真正可用**(1.0.0 遗留)
- 关键路径阻塞 main.ts 启动:**否**(tsc 错误是 type-level,不阻碍 runtime,`vite build` 通过 esbuild 跳过类型检查)

### 3.4 runtime 风险

- ✓ 主进程/渲染进程 import 边界正确(无 `electron` import 在 src/,无 `window.localStorage` 在主进程)
- ✓ `ScreenVision` / `D1ScreenshotQA` 用 `await import('electron')` 在主进程,正确

### 3.5 持久化数据位置

| 模块 | 路径 |
|---|---|
| ChatConfig | `userData/chat-config.json` |
| SkillEffectivenessTracker | `userData/skill-stats.json` (W6) |
| SelfLearner | `userData/hermes-learning/` |
| ModelConfig | `userData/models.json` |
| CostTracker | `userData/cost-log.json` (W5) |
| AgentCheckpoint | `userData/checkpoints/{id}.json` (W5) |
| AutoCreator | `userData/skills/{name}/` (W6) |
| TaskLog | `userData/task-log.json` |
| **SkillManager** | `userData/skills/` ⚠️ 与 AutoCreator 冲突 |
| **SkillLoader** | `userData/skills/` ⚠️ 与 AutoCreator 冲突 |
| HermesMemory | `userData/hermes-memory/` |
| ConfigStore | `userData/config.json` |
| PermissionConfig | `userData/permissions.json` |
| LogManager | `userData/logs/` |

⚠️ **`skills/` 三处独立注册**:`SkillManager.skillsDir` / `SkillLoader.skillsDir` / `AutoCreator.skillsDir` 可能 race condition / 路径漂移(取决于 cwd)

⚠️ **未 namespace 化**:W1-W6 直接复用 1.0.0 扁平文件命名。W7+ 多域并发启动后,文件数量会爆炸。

---

## 维度 4:预存问题清单

### 4.1 必修(阻塞 W7+)

| # | 问题 | 影响 | 修复成本 |
|---|---|---|---|
| 1 | **main.ts 未启动任何 W3+ 子系统** | W7+ 跨域通信全失败 | 1 文件 / ~50 行 |
| 2 | **registerD1ScreenshotShortcut 未被 main.ts 调** | D1 demo 全链路失效 | 1 文件 / 2 行 |
| 3 | **D5RecordingToSkill 未注册 SkillRuntime,无 IPC 入口** | D5 demo 不可点 | 2 文件 / ~20 行 |
| 4 | **W3.3 IPC 13 handler 全 stub + 0 caller** | W7 Channel/Sandbox/Insight 调用要从零做 | 1 commit (W7 同步 wire) |
| 5 | **Vue Router 只 4 route,9 个 view 不可达** | W7+ 这些视图必须挂上 | 1 文件 router + SideNav / ~60 行 |
| 6 | **TaskStep / StepType 不匹配 28 个 tsc 错** | W7 Chat 流 → Task 链路集成测试暴露 | 1 文件 / ~40 行 |
| 7 | **SkillManager 三处 skillsDir** | 持久化读取失败 / 路径漂移 | 3 文件 / ~15 行 |
| 8 | **CapabilityRegistry 0 caller / 0 register** | 违反 spec 段 4 跨域通信原则 | W7 启动时一次性 wire 5-10 个 domain |
| 9 | **Runtime 5 子系统 0 实例化** | W7+ 多 Agent 并发不可用 | W7 启动时 5 行 wire |
| 10 | **W3-W6 测试覆盖 ≈ 0** | W7+ 重构无安全网 | W7 任务强制每子任务 ≥1 test |

### 4.2 可延后

| # | 问题 | 触发条件 |
|---|---|---|
| 1 | `EmbeddingService` 用本地 hash(W6 stub) | W8+ 接真实 LLM embedding |
| 2 | `MemoryVectorStore` 内存 Map,未持久化 | memory > 1000 时 |
| 3 | `ScreenVision.maxFrames = 60` 写死 | W8+ 长录屏 |
| 4 | `ScreenVision` 帧 dataUrl 内存占用大 | W8+ 接 ffmpeg |
| 5 | `CapabilityRegistry.execute()` 依赖缺失只 warn | W7+ 加严格模式 |
| 6 | `EventBus.maxHistory = 100` 写死 | 链路追溯需要更长 history |
| 7 | `EventBus.subscribe` 不返回 Promise | W8+ 需要 async unsubscribe |
| 8 | `IpcServer.destroy()` 用 `Object.keys` 清空,会误删 IpcBridge 等 | W7+ 想选择性清理 |
| 9 | `vue-tsc` 19 个 vue/element-plus/i18n 渲染端错误 | W9+ 加 vue-i18n |
| 10 | W3.3 IPC handler 用了 `_: any` 弱类型 | W7 重写时一并收紧 |

### 4.3 不必修

1. **W2.1 tokens.css Apple HIG 颜色值微调** — 视觉调整
2. **W1.3 vite-plugin-electron CJS API deprecation warning** — 来自 vite 内部,不阻塞 build
3. **screen-capture-recorder 未集成** — W7 之前不需要
4. **vue-tsc 19 错** — 1.0.0 遗留,W9 前不修也行
5. **OpenClawGateway `child_process.exec` 旧 API** — 1.0.0 路径,W7+ 重构时一起换

### 4.4 风险评分

**W7 可继续性:5/10**(中等风险,有条件可继续)

**评分依据**:
- ✓ 工具链就位 / 8 能力域骨架完整 / 命名规范统一 / 0 新 npm 依赖
- ✗ **致命**:main.ts 没串接任何 W3+ 子系统,当前 build 起来只是 1.0.0 老应用套了 W3+ 装饰层
- ✗ 渲染端 9 个 view 不可达(router 未注册)
- ✗ W3.3 13 IPC handler 全 stub,0 caller
- ✗ 31 个 W1-W6 引入的 tsc 错误未消
- ✗ 测试覆盖 ≈ 0(W3-W6 新文件)

---

## 总结

### 优势

1. **架构蓝图清晰**:`contracts/types.ts` 7 个域入口接口完整,`CapabilityRegistry` 骨架规范,W7+ 直接 wire 真实现即可
2. **Runtime 5 子系统组件齐备**:EventBus 38 个 publish 调用方活跃,SkillRuntime / Conversation / Actor 等是 W7+ 多 Agent 并发调度的现成底座
3. **代码质量基线统一**:189 处 `: any` 多集中在 1.0.0 老文件,W3-W6 新文件 type 完备性更高;`@ts-ignore` 仅 1 处
4. **测试框架就位**:vitest 1.6 + jsdom + 71/71 ✓,W7 任务可立即补单元测试
5. **0 新增 npm 依赖**:W1-W6 全用 Electron 内置 API,W7+ 仍可保持轻依赖

### 劣势

1. **致命 — main.ts 未串接任何 W3+ 子系统**:孤儿代码,build 起来只是 1.0.0 老应用套了 W3+ 装饰层
2. **W3.3 IPC 13 namespace + IpcBridge 全 dead code**:渲染端 0 caller
3. **9 个 view 路由不可达**:D1/D5 demo 不可点
4. **D1/D5 demo 完全跑不通**:registerD1ScreenshotShortcut / D5 builtin / D1+D5 router 全没接
5. **测试覆盖 ≈ 0**:W3-W6 引入 30+ 文件 0 test
6. **31 个 W1-W6 新 tsc 错误未消**:可控但需清理

### W7 启动前建议(5 commit 级修复,~350 行)

| Commit | 内容 | 修复 # | 文件数 | 行数 |
|---|---|---|---|---|
| **W7.0.1 boot wiring** | main.ts `app.whenReady` 内追加 7 个 wire 调用:CapabilityRegistry register × N 域 / HermesAdapter warmup / ChatManager.registerAgent / registerD1ScreenshotShortcut / ensureD1SkillRegistered / IpcBridge.registerHandler / Scheduler warmup | #1, #2, #3, #8, #9 | 1 | ~50 |
| **W7.0.2 router + D5 IPC** | src/router/index.ts 追加 10 个 route + SideNav 菜单项 + preload 暴露 d5:run IPC + main.ts 调 runD5 IPC handler | #3, #5 | 3 | ~80 |
| **W7.0.3 tsc 收敛** | tsconfig.node.json 加 target: ES2022 + TaskExecutor 扩 StepType enum + CapabilityRegistry 加类型注解 + ChatManager W4.6 stream 段消除 description 引用 | #6 | 5-6 | ~60 |
| **W7.0.4 SkillManager 路径单点化** | SkillManager 抽 `public static getSkillsDir()` 静态方法,替换 SkillLoader / AutoCreator | #7 | 3 | ~15 |
| **W7.0.5 单元测试补底** | 为 EventBus / CapabilityRegistry / HermesAdapter / AgentBrain 各加 1 个 smoke test | #10 | 4 新 | ~150 |

**预计提升 W7 可继续性评分从 5/10 到 8/10**。

---

**关键决策**:W1-W6 实施完成,但**真正的"接通 + 跑通"还需要 W7.0.1 - W7.0.5 五个 commit 兜底**。W7 启动前必须先做这些修复,否则 W7 一开工就要先做 boot 接线,挤占 W7 任务时间。