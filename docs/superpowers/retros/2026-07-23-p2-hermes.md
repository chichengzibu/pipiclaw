# P2 Hermes 2.0 Phase Retro (2026-07-23)

**整合到** `2026-07-23-v4-100pct.md` 的 P2 段。

## 摘要

P2 单元层全部完成(17 个测试覆盖 T2.1 / T2.2 / T2.3 / T2.4),T2.5(1 周真实用户回放)需 1+ 周使用,留 follow-up。

## 关键产出

- `tests/integration/hermes-memory.test.ts` — 8 个 test
  - 核心记忆 / 经验记忆 store + recall + buildMemoryPrompt
  - 落盘 USER.md + MEMORY.md
  - 重复 N 次操作 retrieve
- `tests/integration/hermes-self-learner.test.ts` — 9 个 test
  - observeExecution 去重 + 阈值 + 截断
  - saveSkillFromProposal 落盘 skill.md
  - SkillLoader 热加载

## 经验

1. SelfLearner 写入路径 `getAppPath() + '/../skills'`,SkillLoader 读取 `getPath('userData') + '/skills'`
   - 测试 mock 让 getAppPath 返回 `userData/app` 子路径,让 `../skills` 解析到 userData/skills
2. skill.md 必须 `# name + ## 描述 + ## 触发关键词 + ## 操作步骤` 才能被 parseSkillMd 解析
3. observeExecution **去重**(同 instruction 只算一次),与原 plan 略不同 — 实际是观察 unique patterns

## 留 follow-up

- T2.5:1 周真实用户回放
- T2.2 真 LLM 提案生成(目前 test 用手动构造 proposal,真流程要 `analyzeAndGenerateSkill` + 调 LLM)

完整 P0-P5 总收尾见 `2026-07-23-v4-100pct.md`。
