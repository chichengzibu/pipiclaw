# P4 UX 抛光 Phase Retro (2026-07-23)

**整合到** `2026-07-23-v4-100pct.md` 的 P4 段。

## 摘要

P4 重点 T4.2(错误人话化)完成。T4.1(FirstLaunchGuide 优化)/ T4.3(性能)/ T4.4(暗色模式实际主题)/ T4.5(多窗口)留后续 PR。

## 关键产出

- `src/utils/humanizeError.ts` + `tests/unit/humanizeError.test.ts`(15 test)
  - 9 类错误模式:network / auth / permission / rate-limit / config / not-found / oom / unknown
  - 每类带 userMessage(中文)+ hint(下一步)+ action(route 跳转)
  - 非 Error 对象(null/undefined/number)也安全处理

## 已有但未优化的

- FirstLaunchGuide 组件已存在(`src/components/guide/FirstLaunchGuide.vue`)
- 5 套浅色主题已就位(`theme-warm-tech` 等),缺 dark 模式实际主题
- bundle size 1.14MB,需 code-split

## 留 follow-up

- T4.1 FirstLaunchGuide 改 3 步引导(选 provider → 测连接 → 试对话)
- T4.3 性能 code-split,目标 < 800KB
- T4.4 暗色模式实际主题
- T4.5 多窗口 / 标签页
- T4.2 集成进 UI(toast / 弹窗显示 humanize 后的错误)

完整 P0-P5 总收尾见 `2026-07-23-v4-100pct.md`。
